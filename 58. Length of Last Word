class Solution:
    def lengthOfLastWord(self, s):
        """
        :type s: str
        :rtype: int
        """
        if s.strip():
            return len(s.split()[-1])
        else:
            return 0
